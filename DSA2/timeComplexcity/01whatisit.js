// time complexity  tell us how much time it will take to execute certain algorithms

function sum(arr) {
    let sum = 0;   // 1 time
    for (let i = 0; i < arr.length; i++) {
        sum += num[i]                // n time
    }
    return sum; // 1 time
}

// it will take n + 2
// this is  O(n+2)

// but we called O(n)

/*
 there are 3 type of time complexity 
 1. best case
 2. average case
 3. worst case
*/
[1,2,5]
